import DS from 'ember-data';

// var <%= camelizedModuleName %> = DS.Model.extend({
export default DS.Model.extend({
  // example_number_id: DS.attr('number'),
  // example_string_id: DS.attr('string'),
  // example_association: DS.belongsTo('other_thing', { embedded: 'always' })
  // example_association: DS.hasMany('other_thing', { embedded: 'always' })
  // exampled_property: function() {
  //   var thing = this.get('thingtowatch');
  //   if (thing === true){
  //    return true; 
  //   }else{
  //    return false; 
  //   }
  // }.property('thingtowatch'), 
});
// export default <%= camelizedModuleName %>;
